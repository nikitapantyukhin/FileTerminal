# Лабораторная работа №2
# File Terminal - Мини-оболочка с файловыми командами


## О проекте

File Terminal - это полнофункциональная мини-оболочка, реализующая все основные команды для работы с файловой системой. Проект разработан на чистом Python и предоставляет удобный интерфейс для выполнения повседневных файловых операций с поддержкой истории, отмены действий и логирования.

### Ключевые особенности

-  **Полный набор файловых команд** - от базовых операций до работы с архивами
-  **Система отмены действий** - возможность отката операций cp, mv, rm
-  **Автоматическое логирование** - все команды и ошибки записываются в лог-файл
-  **Продвинутый поиск** - рекурсивный поиск по содержимому файлов с поддержкой regex
-  **Работа с архивами** - создание и распаковка ZIP и TAR.GZ архивов
-  **История команд** - просмотр и управление историей введенных команд

## Установка и запуск

### Требования

- Python 3.8 или выше
- Доступ к файловой системе

### Запуск проекта

```bash
# Клонирование репозитория
git clone https://github.com/nikitapantyukhin/FileTerminal.git
# Переход к папке с проектом
cd FileTerminal

# Запуск терминала
python3 -m src.main
```

### Быстрый старт

После запуска вы увидите приглашение командной строки:

```
Добро пожаловать в файловый терминал.
Введите 'exit' для выхода или нажмите Ctrl + D

/home/user/FileTerminal$ 
```

## Реализованные команды

### Базовые файловые операции

| Команда | Синтаксис | Описание | Флаги |
|---------|-----------|-----------|--------|
| `ls` | `ls [-l] [path...]` | Просмотр содержимого директории | `-l` - подробный вывод |
| `cd` | `cd <path>` | Смена текущей директории | - |
| `cat` | `cat <file>...` | Вывод содержимого файлов | - |
| `cp` | `cp <source> <destination>` | Копирование файлов и папок | - |
| `mv` | `mv <source> <destination>` | Перемещение/переименование | - |
| `rm` | `rm [-r] <path>...` | Удаление файлов и папок | `-r` - рекурсивно |

### Архивация и поиск

| Команда | Синтаксис | Описание | Флаги |
|---------|-----------|-----------|--------|
| `grep` | `grep [-r] [-i] <pattern> <path>` | Поиск по содержимому | `-r` - рекурсивно, `-i` - игнорировать регистр |
| `zip` | `zip <folder> <archive.zip>` | Создание ZIP архива | - |
| `unzip` | `unzip <archive.zip>` | Распаковка ZIP архива | - |
| `tar` | `tar <folder> <archive.tar.gz>` | Создание TAR.GZ архива | - |
| `untar` | `untar <archive.tar.gz>` | Распаковка TAR.GZ архива | - |

### Утилиты и управление

| Команда | Синтаксис | Описание | Параметры |
|---------|-----------|-----------|-----------|
| `history` | `history [N]` | Просмотр истории команд | `N` - количество последних команд |
| `undo` | `undo` | Отмена последней операции | - |
| `exit` | `exit` | Выход из терминала | - |

## Примеры использования

### Работа с файлами и директориями

```bash
# Просмотр содержимого текущей директории
/home/user$ ls
documents  downloads  photos  scripts

# Подробный просмотр с правами доступа и размерами
/home/user$ ls -l
drwxr-xr-x  2     4096 Oct 31 12:00 documents
-rw-r--r--  1     1024 Oct 31 11:30 notes.txt

# Переход в директорию
/home/user$ cd Desktop

# Просмотр содержимого файла
/home/user/documents$ cat test.txt

# Копирование файла
/home/user/documents$ cp 123.txt 12345

# Рекурсивное копирование директории
/home/user$ cp -r folder main_folder
```

### Поиск и фильтрация

```bash
# Поиск текста в файле
/home/user$ grep "text" main.py

# Рекурсивный поиск во всех файлах
/home/user$ grep -r "text" src/

# Поиск без учета регистра
/home/user$ grep -i "text" test.txt
```

### Работа с архивами

```bash
# Создание ZIP архива
/home/user$ zip documents documents.zip

# Распаковка архива
/home/user$ unzip documents.zip

# Создание TAR.GZ архива
/home/user$ tar photos photos.tar.gz
```

### Управление историей и отмена действий

```bash
# Просмотр последних 10 команд
/home/user$ history 10

# Отмена последней операции (копирования, перемещения или удаления)
/home/user$ undo
```

## Логирование

Все операции автоматически записываются в файл `shell.log` в формате:

```log
[2025-10-31 14:30:22] ls -l Desktop
[2025-10-31 14:30:25] cp qwerty.py 12345
[2025-10-31 14:30:27] ERROR: test.txt
```

**Формат лога:**
- Дата и время выполнения команды
- Полный текст введенной команды
- Сообщения об ошибках (если есть)

## Система отмены действий

### Поддерживаемые операции для отмены:

| Операция | Действие при отмене | Особенности |
|----------|---------------------|-------------|
| `cp` | Удаление скопированного файла/папки | Сохраняет оригинальные файлы |
| `mv` | Возврат в исходное расположение | Обратное перемещение |
| `rm` | Восстановление из корзины `.trash` | Безопасное удаление |

## Тестирование

Проект включает комплексные тесты для всех команд:

```bash
# Запуск всех тестов
python3 tests/test.py
```

### Тестовое покрытие включает:

- ✅ Базовые операции с файлами
- ✅ Обработка ошибок и исключительных ситуаций
- ✅ Рекурсивные операции
- ✅ Работа с архивами
- ✅ Система отмены действий
- ✅ Поиск по содержимому

## Разработка

### Архитектура проекта

Проект построен по модульному принципу:

1. **Модуль парсера** (`parser.py`) - разбор введенных команд
2. **Диспетчер команд** (`execution_command.py`) - маршрутизация команд
3. **Специализированные модули** - реализация конкретных команд
4. **Система логирования** - запись действий и ошибок
5. **Менеджер истории** - управление историей команд и операций

```

## 🐛 Обработка ошибок

Система включает обработку следующих типов ошибок:

- ❌ Файл или директория не существует
- ❌ Неправильные аргументы команд
- ❌ Отсутствуют права доступа
- ❌ Невозможность выполнить операцию
- ❌ Неподдерживаемые флаги команд

Все ошибки логируются и выводятся пользователю в понятном формате.
